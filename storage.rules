rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Cada usuario solo puede leer/escribir en su propia carpeta
    match /profilePictures/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
